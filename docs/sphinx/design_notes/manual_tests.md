# Manual Tests

TODO: Implement automatic tests for GUI.

- Test that all 5 curves can be plotted correctly.
  - Sample curves:
    - `gamma(x)`
    - `-1/x`
    - `log10(x)`
    - `gamma(x+0.5i)`
    - `x**100`
- Test interaction with the plot.
  - Zoom in and out with mouse wheel.
  - Pan the plot by dragging with the left mouse.
  - Select a region to show with left mouse drag with Ctrl key.
  - Check coordinates of the cursor in the status bar.
- Test collapsing and expanding of settings.
- Test fixed image size.
- Test some commands.
  - Save the plot as an image file.
