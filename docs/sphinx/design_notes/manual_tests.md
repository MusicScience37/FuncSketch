# Manual Tests

- Test that all 5 curves can be plotted correctly. (In system tests too.)
  - Sample curves:
    - `gamma(x)`
    - `-1/x`
    - `log10(x)`
    - `gamma(x+0.5i)`
    - `x**100`
- Test interaction with the plot. (In system tests too.)
  - Zoom in and out with mouse wheel.
  - Pan the plot by dragging with the left mouse.
  - Select a region to show with left mouse drag with Ctrl key.
  - Check coordinates of the cursor in the status bar.
- Test collapsing and expanding of settings. (In system tests too.)
- Test fixed image size. (In system tests too.)
- Test some commands. **(Not in system tests.)**
  - Save the plot as an image file.
